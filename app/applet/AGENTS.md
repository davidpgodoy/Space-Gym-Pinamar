# Custom Agent Instructions

- **Do NOT delete, modify, or overwrite the `assets/` folder.** The user manages the images directly on GitHub.
- The `assets/` folder has been added to `.gitignore` so that AI Studio's git integration does not stage deletions for images that exist on GitHub but not in this environment.
